spring:
  mvc:
    servlet:
      path: /api
  jackson:
    mapper:
      accept-case-insensitive-properties: true
  datasource:
    url: ${db.url:jdbc:postgresql://127.0.0.1:5432/dz}
    username: ${db.username:postgres}
    password: ${db.password:5933}
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: true
      connection-timeout: 600000
      maximum-pool-size: 50
logging:
  file:
    max-history: 20