+Student(
    id serial,
    idd int,
    first_name varchar(50),
    middle_name varchar(50),
    last_name varchar(50),
    passport varchar(15),
    birth_date timestamp,
    create_date timestamp,
    delete_date timestamp,
    status varchar(10)
)

+Teacher(
    id serial,
    idd int,
    first_name varchar(50),
    middle_name varchar(50),
    last_name varchar(50),
    passport varchar(15),
    degree varchar(25),
    birth_date timestamp,
    create_date timestamp,
    delete_date timestamp,
    status varchar(10)
)

?User()

+Course(
    id serial,
    idd int,
    name varchar(100),
    description varchar(500),
    teacher_idd int,
    max_count_student int,
    start_date timestamp,
    end_date timestamp,
    create_date timestamp,
    delete_date timestamp,
    status varchar(10)
)

+Student_to_course(
    id serial,
    student_idd int,
    course_idd int,
    success boolean
)

+Room(
    id serial,
    idd int,
    number varchar(5),
    block varchar(5), --Корпус
    max_students int, --В changelog-1 данного поля нет
    create_date timestamp,
    delete_date timestamp
)

+Lesson(
    id serial,
    name varchar(100),
    description varchar(1000),
    course_idd int,
    room_idd int,
    lesson_date_start timestamp,
    lesson_date_end timestamp
)

+Instrument_to_lesson(
    id serial,
    instrument_idd int,
    lesson_idd int
)

+Instrument_to_room(
    id serial,
    instrument_idd int,
    room_idd int
)

+Instrument(
    id serial,
    idd int,
    name varchar(50), --В changelog-1 данного поля нет
    number varchar(), --Инвертарный номер. В changelog-1 varchar(50)
    create_date timestamp,
    delete_date timestamp
)