<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:pro="http://www.liquibase.org/xml/ns/pro"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.9.xsd
    http://www.liquibase.org/xml/ns/pro http://www.liquibase.org/xml/ns/pro/liquibase-pro-3.9.xsd ">

    <changeSet id="20200622-12" author="kapitanov">
        <preConditions>
            <not>
                <tableExists tableName="lessons" schemaName="public"/>
            </not>
        </preConditions>
        <comment>Create table lessons</comment>
        <sql>
            create table lessons (
            id serial,
            name varchar(100),
            description varchar(1000),
            course_idd int,
            room_idd int,
            lesson_date_start timestamp(0),
            lesson_date_end timestamp(0),
            extra_instruments varchar(1000),
            constraint pk_lessons_id primary key(id),
            constraint fk_lessons_course foreign key(course_idd)
            references courses(id),
            constraint fk_lessons_room foreign key(room_idd)
            references rooms(id)
            );
        </sql>
        <rollback>
            <sql>
                drop table lessons;
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>